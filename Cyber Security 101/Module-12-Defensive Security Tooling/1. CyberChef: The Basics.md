# CyberChef: The Basics

## What is CyberChef?

CyberChef is a web-based tool used to perform a wide variety of data manipulation, encoding, decoding, encryption, decryption, and forensic operations.

It works using **recipes**, where multiple operations are executed sequentially.

Think of it as a **Swiss Army Knife for cybersecurity**.

---

# Accessing CyberChef

## Online

Open directly in a web browser.

## Offline

Download the latest release and run it locally on Windows or Linux.

---

# CyberChef Interface

CyberChef consists of four main areas:

## 1. Operations

Contains all available operations grouped into categories.

Examples:

- Base64 Encode/Decode
- URL Encode/Decode
- ROT13
- XOR
- AES
- RSA
- Hashing
- Extractors

A search bar helps quickly find operations.

---

## 2. Recipe

The Recipe is the heart of CyberChef.

It contains the sequence of operations to perform.

Features:

- Add operations
- Rearrange operations
- Configure operation options
- Save recipe
- Load recipe
- Clear recipe
- BAKE! (Execute recipe)
- Auto Bake (Automatically execute after changes)

---

## 3. Input

Where data is provided.

Supports:

- Typing
- Pasting
- Drag-and-drop files
- Uploading folders
- Multiple input tabs

---

## 4. Output

Displays the processed result.

Features:

- Save output
- Copy output
- Replace input with output
- Maximize output pane

---

# Thought Process

When using CyberChef, follow these steps:

1. Define your objective.
2. Provide the input data.
3. Select the required operation(s).
4. Verify whether the output matches the expected result.

If not, modify the recipe and try again.

---

# Common Operations

## Encoding / Decoding

- Base64
- Base58
- Base62
- Base85
- Hex
- Decimal
- URL Encode/Decode
- ROT13

---

## Extractors

Used to extract useful information from text.

Examples:

- Extract IP Addresses
- Extract URLs
- Extract Email Addresses

---

## Date & Time

Useful for timestamp conversion.

Operations:

- To UNIX Timestamp
- From UNIX Timestamp

---

# What is Base Encoding?

Base encoding converts binary data into printable ASCII characters.

Examples:

- Base64
- Base58
- Base62
- Base85

It is used for data transmission and storage—not encryption.

---

## Base64 Example

```
Welcome
↓
V2VsY29tZQ==
```

Decoding:

```
V2VsY29tZQ==
↓
Welcome
```

---

# Manual Base64 Conversion (Summary)

Example:

```
THM
```

Steps:

1. Convert characters to binary.
2. Combine into 24 bits.
3. Split into 6-bit groups.
4. Convert each group to decimal.
5. Map decimals to the Base64 table.

Result:

```
THM
↓
VEhN
```

---

# URL Encoding

URL encoding replaces special characters with hexadecimal values prefixed by `%`.

Examples:

| Character | Encoded |
|-----------|----------|
| : | %3A |
| / | %2F |
| . | %2E |
| = | %3D |
| # | %23 |

Example:

```
https://example.com
```

↓

```
https%3A%2F%2Fexample%2Ecom
```

---

# Common Use Cases

- Decode Base64 strings
- Decode URL-encoded text
- Convert Hex ↔ ASCII
- Convert timestamps
- Extract IPs, URLs, and emails
- Perform encryption/decryption
- Analyze forensic data
- Build multi-step data transformation recipes
